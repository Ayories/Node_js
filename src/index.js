const greet = (name) => {    
    console.log(`hello ${name} how are you doing`)
}
// greet(David)
const calculator = (name,a,b) => {
    greet(David)
    console.log(`hello ${name} how are you doing`)
}
